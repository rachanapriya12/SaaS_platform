# Baked into production builds (Vite). Override in Vercel → Environment Variables if needed.
# Must match your Render Web Service URL exactly (copy from Render → your service → URL).
# If deploy fails or login breaks, replace with the URL shown in the Render dashboard.
VITE_API_BASE=https://saas-platform-v4o3.onrender.com
