# Local dev
VITE_API_BASE=http://localhost:4000

# Production: see frontend/.env.production (committed) or set VITE_API_BASE in Vercel.
# Must be your Render API origin with no trailing slash, e.g. https://your-service.onrender.com
